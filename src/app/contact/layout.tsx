export const metadata = {
  title: "Contact Us | ByteHubble",
  description: "Get in touch with the ByteHubble team. We're here to help with your database and AI infrastructure needs.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

